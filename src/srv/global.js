var path = require('path');

/** @type {Object} - global.swot is a global vars namespace for this Back-End. */
global.swot = {};

/** @type {String} - absolute path to the Back-End (src/srv) directory. */
swot.SERVER_DIR = __dirname + '/';

/** @type {String} - absolute path to the vendor directory for this Back-End. */
swot.VENDOR_DIR = __dirname + '/vendor/node_modules/';
