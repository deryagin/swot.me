/** @type {Object} - global.swot is a global vars namespace for this Back-End. */
module.exports = {
  
  /** @type {String} - absolute path to this Back-End directory. */
  SERVER_DIR: __dirname + '/',

  /** @type {String} - absolute path to the vendor directory for this Back-End. */
  VENDOR_DIR: __dirname + '/vendor/node_modules/'
};
